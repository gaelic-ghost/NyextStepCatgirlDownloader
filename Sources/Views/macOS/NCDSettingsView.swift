import SwiftUI

struct NCDSettingsView: View {
    var body: some View {
        Form {
            Text("NyextStep Catgirl Downloader settings are coming soon.")
        }
        .padding()
        .frame(width: 360)
    }
}
