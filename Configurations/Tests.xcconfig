#include "Shared.xcconfig"

// Unit-test bundle settings that should not vary by build configuration.
PRODUCT_BUNDLE_IDENTIFIER = com.galewilliams.nyextstep-catgirldownloader.tests
GENERATE_INFOPLIST_FILE = YES
